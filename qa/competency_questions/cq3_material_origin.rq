# Catalogue task: What is the origin of the biological material?
PREFIX EVORAO: <https://w3id.org/evorao/>
PREFIX dct: <http://purl.org/dc/terms/>

SELECT ?resource ?collectionTitle
WHERE {
  ?resource EVORAO:collection ?collection .
  ?collection dct:title ?collectionTitle .
}
ORDER BY ?resource
